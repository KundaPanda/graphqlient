[tool.poetry]
name = "graphqlient"
version = "0.0.1a1"
description = ":)"
authors = ["Vojtěch Dohnal <vojdoh@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
gql = {extras = ["aiohttp", "requests"], version = "^3.1.0"}
Jinja2 = "^3.1.1"
inflection = "^0.5.1"

[tool.poetry.dev-dependencies]
pytest = "^7.1.1"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -s"
testpaths = [
    "tests",
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

